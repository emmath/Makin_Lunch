<%= form_for(@ingredientship) do |f| %>
  <% if @ingredientship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ingredientship.errors.count, "error") %> prohibited this ingredientship from being saved:</h2>

      <ul>
      <% @ingredientship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br />
    <%= f.number_field :amount %>
  </div>
  <div class="field">
    <%= f.label :style %><br />
    <%= f.text_field :style %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
