<%= form_for(@lunch) do |f| %>
  <% if @lunch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lunch.errors.count, "error") %> prohibited this lunch from being saved:</h2>

      <ul>
      <% @lunch.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :occasion %><br />
    <%= f.text_field :occasion %>
  </div>
  <div class="field">
    <%= f.label :food %><br />
    <%= f.collection_select :food_id, Food.find(:all), :id, :vegetable, :prompt => "Choose a Veggie!" %>
  </div>
  <div class="field">
    <%= f.label :meal%><br />
    <%= f.collection_select :meal_id, Meal.find(:all), :id, :dish, :prompt => "Which Meal?" %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.datetime_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
